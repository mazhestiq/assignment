<Window x:Class="Assignment.UI.TodoListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:validations="clr-namespace:Assignment.UI.Validations" xmlns:ui="clr-namespace:Assignment.UI" d:DataContext="{d:DesignInstance Type=ui:TodoListViewModel}"
        mc:Ignorable="d"
        Height="150" Width="800">
    <StackPanel Margin="10">
        <Label Content="Title:"/>
        <TextBox Validation.ErrorTemplate="{StaticResource ErrorTemplate}" >
            <TextBox.Text >
                <Binding Path="Title" UpdateSourceTrigger="PropertyChanged">
                    <Binding.ValidationRules>
                        <validations:ToDoListTitleValidationRule />
                    </Binding.ValidationRules>
                </Binding>
            </TextBox.Text>
        </TextBox>

        <Button Content="Save" Command="{Binding SaveCommand}"/>
        <Button Content="Cancel" Command="{Binding CloseCommand}"/>
    </StackPanel>
</Window>
